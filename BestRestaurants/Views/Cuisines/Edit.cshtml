@{
  Layout = "_Layout";
}

@model BestRestaurants.Models.Cuisine

<h4>Edit : @Html.DisplayFor(model => model.Name)</h4>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.CuisineId)

    @Html.LabelFor(model => model.Name)
    @Html.EditorFor(model => model.Name)

    <input type="submit" value="Save" />
}
<p>@Html.ActionLink("Back to Cuisines", "Index", null, htmlAttributes: new {@class="btn btn-primary btn-lg"})</p>